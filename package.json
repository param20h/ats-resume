{
  "name": "ats-resume-builder",
  "version": "1.0.0",
  "description": "AI-powered resume builder with ATS scoring capability",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build"
  },
  "keywords": [
    "resume",
    "ats",
    "ai",
    "react",
    "nodejs"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
